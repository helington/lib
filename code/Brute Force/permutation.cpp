#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define endl '\n'
using vi = vector<int>;
using pii = pair<int, int>;

signed main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0);

    vi v;
    sort(v.begin(), v.end());
    do {
        // fazer o que tiver na permutação atual
    } while (next_permutation(v.begin(), v.end()));

    return 0;
}